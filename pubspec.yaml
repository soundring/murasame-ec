name: murasame_ec
description: ec app

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

  flutter_hooks: ^0.17.0
  hooks_riverpod: ^0.14.0+4

  state_notifier: ^0.7.0

  json_serializable: ^4.1.3
  freezed_annotation: ^0.14.2

  http: ^0.13.3

dev_dependencies:
  flutter_lints: ^1.0.4
  flutter_test:
    sdk: flutter
  build_runner: ^2.0.6
  freezed: ^0.14.2

flutter:
  uses-material-design: true

  assets:
    - product_data/images/
    - product_data/furniture.json
